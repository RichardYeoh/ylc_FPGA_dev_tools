# remove_debug_flags 任务会话记录

## 会话元信息

- 时间：2026-04-17
- 项目路径：`E:\proj\ylc_FPGA_dev_tools\remove_debug_flags`
- 主题：开发 RTL 源码 debug 标记清理 GUI 工具
- 记录模式：已启用

## 对话原文

### User

在remove_debug_flags下做一个新项目：去除rtl中的debug标记。我使用的是vivado一套工具链，多用v。用MARK_DEBUG的关键字在源码中标记debug信号，如：`(* MARK_DEBUG="true" *)reg [15:0] pix_cnt;` 有时也会加上dont_touch，比如：`(* dont_touch="true" *)(* MARK_DEBUG="true" *)reg  [15:0] row_cnt;` 我需要你开发一个具有ui界面的去除rtl源码中debug标记的工具，可以遍历选定的目录，将rtl中的相关标记清除。希望你可以做一些泛化，比如vhdl，以及一些随性的写法都可以自动识别并清除，清除后最好可以让有效代码再对齐一下。注意：除此之外，不要有任何改变rtl设计行为的修改。需要启用记录模式，对话原文记录在 remove_debug_flags下

### Assistant

提出实施计划：在 `remove_debug_flags` 下开发 Python/Tkinter GUI 工具，支持递归扫描 Verilog/SystemVerilog/VHDL，清理 `MARK_DEBUG`，默认仅清理与其伴随出现的 `dont_touch`，提供预览、写回、备份、局部整理和 CLI/GUI 双入口。

### User

`.bak备份放在并行的debug_version目录里，目录结构和原rtl一致`

### Assistant

更新实施计划：写回前默认将备份放入源目录同级 `debug_version`，按源目录内部相对路径保留结构。例如 `rtl\arinc818_src\dvi_final_out_0.v` 备份为 `debug_version\arinc818_src\dvi_final_out_0.v.bak`，已有备份不覆盖，自动追加 `.bak1`、`.bak2`。

### User

Implement the plan.

## 当前实施记录

- 新增 `remove_debug_flags.py`：清理核心、CLI 与 Tkinter GUI。
- 新增 `run_remove_debug_flags_gui.bat`：Windows GUI 启动脚本。
- 新增 `用户使用说明.md` 与 `工具开发说明.md`。
- 新增 `test_remove_debug_flags.py`：覆盖 Verilog、VHDL、伴随 `dont_touch` 和并行 `debug_version` 备份结构。

## 验证记录

- `python -m py_compile .\remove_debug_flags\remove_debug_flags.py`：通过。
- `python -m unittest discover -s .\remove_debug_flags -p "test_*.py"`：5 个测试通过。
- `python .\remove_debug_flags\remove_debug_flags.py .\remove_debug_flags\rtl --check`：预览扫描 20 个候选 RTL 文件，命中 9 个文件，预计删除 `MARK_DEBUG` 98 个、配套 `dont_touch` 24 个，未写回源码，未生成备份。
- `python .\remove_debug_flags\remove_debug_flags.py --help`：CLI 参数输出正常。
- `git diff --check`：通过。

### User

备份目录不让用户选择，告知就行

## 修订记录

- GUI 中备份目录改为只读提示，不再提供输入框或“选择备份”按钮。
- CLI 移除 `--backup-root` 参数，统一使用源目录同级 `debug_version`。
- 使用说明和开发说明同步更新为固定备份目录策略。
- 回归验证：`py_compile` 通过，`unittest` 5 项通过，`--help` 中已无 `--backup-root`，样例 RTL 预览扫描结果保持 20 候选、9 命中、预计删除 98 个 `MARK_DEBUG` 与 24 个配套 `dont_touch`，`git diff --check` 通过。
