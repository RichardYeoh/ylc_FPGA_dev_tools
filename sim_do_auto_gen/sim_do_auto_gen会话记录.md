# sim_do_auto_gen会话记录

## 2026-04-07 工具通用性复盘与功能泛化方向梳理

- 时间：`2026-04-07`
- 主题：对照项目根目录文档、`20260407` 实战记录与脚本差异，分析 `sim_do_auto_gen` 当前设计边界，并整理后续泛化方向

### 本次工作摘要

- 检查了项目根目录文档：
  - `仿真do脚本自动生成.md`
  - `工具开发说明书.md`
  - `用户指导手册.md`
  - `团队培训精简手册.md`
- 检查了实战落地记录：
  - `20260407/sim_do_auto_gen/v_hdmi_tx_ss_0_ex_818sim会话记录.md`
- 对比了根目录与 `20260407/sim_do_auto_gen` 下的 `gen_my_sim.ps1` 差异
- 识别出当前工具的主要固定假设：
  - 面向 `Vivado` 导出的 `Questa/ModelSim` 仿真目录
  - 默认目录拓扑为 `*.sim/sim_1/behav/{questa|modelsim}`
  - 输出固定写到所选目录的上一级
  - 输入来源固定依赖 `*_compile.do`、`*_elaborate.do`、`*_simulate.do`、`simulate.bat`
  - 保留逻辑只覆盖用户波形块与最后一条 `run ...`
- 识别出实战新增能力与文档描述之间的偏差：
  - `20260407` 版本脚本已增加 `$readmemb/$readmemh` 初始化文件 staging 能力
  - 根目录文档尚未把这项能力提升为通用设计的一部分
- 形成一份项目内建议文档：
  - `sim_do_auto_gen功能泛化建议.md`

### 关键结论

- 当前工具的核心价值已经不只是“把 `my_sim.tcl/my_sim.bat` 放到上一级目录”，而是“在不改动 Vivado 原始导出结果的前提下，生成一个更稳定、可继承、可补强的仿真启动包装层”
- `20260407` 实战问题说明：真正需要泛化的不是单独兼容某个 `.bin` 文件，而是把“运行前资源补齐 / staging”抽象成通用机制
- 如果后续要提升通用性，优先级最高的不是继续堆更多目录名判断，而是把以下三层能力做成独立模块：
  - 仿真上下文识别
  - 运行资源扫描与 staging
  - 输出布局与用户保留区策略

### 后续建议

- 优先按 `sim_do_auto_gen功能泛化建议.md` 中的 `P0 -> P1 -> P2` 顺序推进
- 根目录现有四份文档后续应统一改写为“仿真包装脚本生成器”口径，而不是继续限定成“选择 `questa/modelsim` 目录的小工具”

## 2026-04-07 初始化文件歧义候选选择机制实现

- 时间：`2026-04-07`
- 主题：为 `gen_my_sim.ps1` 增加初始化文件工程级搜索与多候选单选确认能力

### 本次改动

- 在 `gen_my_sim.ps1` 中加入初始化文件搜索根目录推导逻辑
- 搜索策略调整为：
  - 先查引用该文件的 RTL 同目录与常见资源目录
  - 再回溯到工程根目录做同名文件搜索
- 当同名初始化文件命中多个候选时：
  - UI 模式弹出单选对话框
  - 默认选中工具推荐项
  - 无界面模式自动采用推荐项并记录日志
- 将该行为同步更新到：
  - `用户指导手册.md`
  - `工具开发说明书.md`

### 验证情况

- 使用项目内临时最小化样例验证了无界面模式：
  - 能识别多个同名 `foo.bin` 候选
  - 能按“与当前 RTL 路径最近”原则推荐同目录候选
  - 生成的 `my_sim.tcl` 中已写入对应的 staging 段
- UI 弹窗路径未做自动化交互验证，但相关表单与回调逻辑已通过脚本语法检查

## 2026-04-07 实际工程回归、`.Count` 修复与版本提交

- 时间：`2026-04-07`
- 主题：在真实工程中继续回归 `sim_do_auto_gen`，修复 PowerShell 单对象 `.Count` 异常，并完成本地版本提交

### 对话摘要

#### User

- 追问 `P0` 方案具体含义，并明确表示本轮只需要推进“运行资源扫描与 staging”这一路
- 进一步强调工具不仅要从 RTL 同目录找初始化文件，还要能顺着用户选中的仿真目录回溯工程层级，在 `src` 等更深路径下自动查找对应文件
- 要求当同名初始化文件存在多个候选时，工具弹出单选对话框，并默认选中工具推荐项
- 在真实工程回归时反馈新报错：`The property 'count' cannot be found on this object`
- 核对生成的 `my_sim.tcl` 中 `mem_init_sync_list` 是否由工具自动推导生成，并要求给出对应代码行号
- 要求执行 `commit and push`
- 最后要求把本次会话同步记录下来

#### Assistant

- 解释了“静态路径可自动覆盖、动态构造路径无法纯静态 100% 覆盖”的边界，并建议把通用目标定为：
  - 先覆盖字面量/半静态场景
  - 再用工程级搜索与推荐机制做兜底
- 在 `gen_my_sim.ps1` 中实现了以下能力：
  - 根据用户选择的仿真目录反推出工程搜索根目录
  - 对 `$readmemb/$readmemh("裸文件名")` 先查 RTL 同目录和常见资源目录，再在工程根下做同名文件搜索
  - 对多候选文件按“与当前引用 RTL 路径最近”进行打分推荐
  - UI 模式弹出单选对话框，默认选中工具推荐项
  - `-NoUi` 模式自动采用推荐项并记录日志
- 同步更新了：
  - `用户指导手册.md`
  - `工具开发说明书.md`
- 在实际工程回归时定位到 `.Count` 报错根因：
  - PowerShell 某些函数在单结果场景下返回单对象而非数组
  - 后续代码直接取 `.Count`，在特定对象类型上会失败
- 对主脚本做了数组收口修复，确保以下结果统一使用 `@(...)` 包装：
  - 路径分段结果
  - 初始化文件候选列表
  - 编译源文件列表
  - 工程级搜索结果
  - staging 项列表
- 再次执行了语法检查与最小样例回归，确认 `.Count` 异常不再出现
- 按用户要求执行了版本提交：
  - 本地 `git commit` 成功
  - 提交号：`2482359`
  - 提交信息：`Enhance memory init staging resolution`
- 尝试 `git push origin main` 两次，均失败于网络层：
  - `Recv failure: Connection was reset`

### 本次涉及的主要文件

- `gen_my_sim.ps1`
- `用户指导手册.md`
- `工具开发说明书.md`
- `sim_do_auto_gen功能泛化建议.md`
- `sim_do_auto_gen会话记录.md`

### 关键结论

- 当前 `my_sim.tcl` 中生成的 `mem_init_sync_list` 属于工具自动解析和生成的结果，不是手工“特调”硬编码
- 真实工程里初始化文件搜索的关键不是盲目全盘扫描，而是：
  - 用 `*_compile.do` 锚定 RTL
  - 以 RTL 路径和工程根为搜索边界
  - 用路径接近度来做候选排序
- PowerShell 脚本里凡是需要按集合处理的函数返回值，后续都应显式数组化，避免单对象/多对象返回差异导致 `.Count` 报错
- 当前远端未完成推送；如网络恢复，只需在仓库根目录再次执行：
  - `git push origin main`
